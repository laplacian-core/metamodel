rootProject.name = "laplacian.model.metamodel"
pluginManagement {
    repositories {
        maven(url = "../mvn-repo/")
        maven(url = "https://github.com/nabla-squared/raw/mvn-repo/")
        gradlePluginPortal()
        jcenter()
    }
}