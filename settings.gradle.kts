rootProject.name = "laplacian.model.metamodel"
pluginManagement {
    repositories {
        maven(url = "../maven2/")
        maven(url = "https://bitbucket.org/nabla2/maven2/raw/master/")
        gradlePluginPortal()
        jcenter()
    }
}
